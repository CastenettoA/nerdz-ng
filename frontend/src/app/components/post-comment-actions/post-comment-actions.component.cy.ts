import { PostCommentActionsComponent } from './post-comment-actions.component'

describe('PostCommentActionsComponent', () => {
  it('should mount', () => {
    cy.mount(PostCommentActionsComponent)
  })
})